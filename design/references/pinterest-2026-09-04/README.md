# Pinterest pass, 2026-09-04

Ten images kept from six searches (agent workflow builder, node editor, document editor, agent ops dashboard, kanban, editorial app). Each is here because it settles something for the v3 direction studies. Everything from the "agent ops dashboard" search was discarded: purple gradients, holograms, mascots, hero metrics, all on the anti-reference list.

| File | What it settles |
|---|---|
| `builder-vertical-steps-branches.jpg` | The workflow builder is a vertical list of step cards with a plus between them and yes/no branches drawn under a condition. Cards are the definition. |
| `builder-palette-plus-flow.jpg` | A palette of step kinds beside the flow. Trigger, wait, branch as distinct card headers. |
| `builder-runs-drawer-under-canvas.jpg` | Runs, triggers, errors and timeline live in a drawer under the definition, not on a separate page. |
| `agent-group-with-tool-nodes-light.jpg` | A role-assignment step shows its tools as children of the agent, on a light ground. |
| `node-inline-input-output.jpg` | A step edits in place with input and output tabs; no side form. |
| `board-light-colour-on-tags.jpg` | Board columns neutral, colour on tags only, on a light flavor. Confirms the board rule. |
| `project-header-properties-view-tabs.jpg` | The project room header: title, one line of properties, then view tabs (Board, Timeline, List, Table). |
| `docs-sidebar-folders-light.jpg` | The docs list is folders and rows, not a type filter. |
| `docs-editor-tree-page-meta.jpg` | Tree, wide page, meta on the right; toolbar appears on selection only. |
| `projects-dense-table-tags.jpg` | A unit's projects as a numbered dense table with tags at the right edge. |

Pin URLs are recorded in the direction studies artifact of the same date.
